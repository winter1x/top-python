def reverse(string):
    if string == None:
        return None
    return string[::-1]

"""
includes()
size()
filter()
...(20 штук)
"""
"""from pydash import collections

coll = ['One', 'Two', 3, 10, 'cat', {}, "", 10, False]
def test_includes():
    assert includes(coll, 3) is True
    assert includes(coll, 11) is False

def test_size():
    assert size(coll) == 9
"""
